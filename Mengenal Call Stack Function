Mengenal Call Stack Function

=================================

https://pythontutor.com/render.html#mode=display

Buat 2 file index.html dan app.js

index.html :

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Asynchronous JavaScript</title>
	</head>
	<body>
		<script src="app.js"></script>
	</body>
</html>

==========================


app.js  :


const hello = async () => {
	// throw 'Maaf gak kenal';
	return 'Hello World';
};

hello()
	.then((res) => {
		console.log('response', res);
	})
	.catch((err) => {
		console.log('error', err);
	});


========================================

contoh lain :

function multiply(a, b) {
  return a * b;
}

function square(a) {
  return multiply(a, a);
}

function main() {
  const result = square(5);
  console.log(result);
}

main();

=================================

Fungsi main() dipanggil, lalu ditambahkan ke atas stack.
Fungsi square(5) dipanggil, lalu ditambahkan ke atas stack.
Fungsi multiply(5, 5) dipanggil, lalu ditambahkan ke atas stack.
Fungsi multiply(5, 5) selesai dijalankan, lalu dihapus dari stack.
Fungsi square(5) selesai dijalankan, lalu dihapus dari stack.
Fungsi main() selesai dijalankan, lalu dihapus dari stack.

====================================
